export type IExercise = {
  name: string;
  load: number;
  unit: string;
  increment: number;
};
export interface colorTheme {
  background: string;
  card: string;
  text: string;
  cardText: string;
  listColor: string;
}
