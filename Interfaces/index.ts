export type IExercise = {
  name: string;
  load: number;
  unit: string;
};
export interface colorTheme {
  background: string;
  card: string;
  text: string;
  cardText: string;
  listColor: string;
}
